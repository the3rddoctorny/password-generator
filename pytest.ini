[pytest]
addopts = -q
testpaths = tests
markers =
    smoke: quick sanity checks
    positive: happy path functional tests
    negative: edge cases / invalid configs
    a11y: accessibility checks (axe + keyboard)

